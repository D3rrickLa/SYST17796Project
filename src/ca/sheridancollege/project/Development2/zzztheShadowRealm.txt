//Where all the banish/ bad/ unused/ ugly-stinky code goes, but we still need a archieve 


--GroupOfCards--
// for (int i=0; i<13; i++)
// {
//   Rank value = Rank.values()[i];

//   for (int j=0; j<4; j++)
//   {
//     StandardPlayingCards card = new StandardPlayingCards(Suits.values()[j], value);
//     this.cards.add(card);
//   }
// }


// Iterator<StandardPlayingCards> cardIterator = cards.iterator();
// while (cardIterator.hasNext())
// {
//   StandardPlayingCards aCard = cardIterator.next();
//   System.out.println(aCard.getSuits() + " of " + aCard.getRanks());
// }


--CardAssignment.java--- (old class)
package Development2;

import java.util.ArrayList;
//this will become gorupofRanks and the genreation will go in here
import java.util.Arrays;

public class CardsAssignment extends Card2 {

    private String[] cards2 = new String[52];
    private ArrayList<Card2> cards;

    public void assignRankstoDeck(){
        
        int counter = 0;
        
        for (Card2.Suits s : Card2.Suits.values()) {
            for (Card2.Rank r : Card2.Rank.values()) {
                super.setRank(r);
                super.setSuits(s);
                cards2[counter] = r + ""+ s;
                
                System.out.println(cards2[counter]);
            }

        }
   
    }

    @Override
    public String toString() {
        return super.getSuits() + " " + super.getRank();
    }
    
}




